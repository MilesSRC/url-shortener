<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>dubgus - Slug Not Found</title>
    <meta name="description" content="That link has either been disabled or doesn't exist">
    <link rel="shortcut icon" href="https://milesr.dev/res/img/logo.png" type="image/x-icon">

    <style>
        html, body {
            background: #121212;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            margin: 0;
            padding: 0;
        }

        div {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #fff;
            font-size: 2.5rem;
            font-weight: 300;
            margin: 0;
            padding: 0;
        }

        p {
            color: #fff;
            font-size: 1.25rem;
            font-weight: 300;
            margin: 0;
            padding: 0;
        }

        a {
            color: #fff;
            font-size: 1.25rem;
            font-weight: 300;
            margin: 0;
            padding: 0;
        }

        a:hover {
            text-decoration: underline;
        }

        @media (min-width: 768px) {
            h1 {
                font-size: 3rem;
            }

            p {
                font-size: 1.5rem;
            }

            a {
                font-size: 1.5rem;
            }
        }

        @media (min-width: 1024px) {
            h1 {
                font-size: 4rem;
            }

            p {
                font-size: 2rem;
            }

            a {
                font-size: 2rem;
            }
        }

        @media (min-width: 1280px) {
            h1 {
                font-size: 5rem;
            }

            p {
                font-size: 2.5rem;
            }

            a {
                font-size: 2.5rem;
            }
        }

        @media (min-width: 1600px) {
            h1 {
                font-size: 6rem;
            }

            p {
                font-size: 3rem;
            }

            a {
                font-size: 3rem;
            }
        }

        @media (min-width: 1920px) {
            h1 {
                font-size: 7rem;
            }

            p {
                font-size: 3.5rem;
            }

            a {
                font-size: 3.5rem;
            }
        }
    </style>
</head>
<body>
    <div>
        <h1 style="font-weight: bold;">Whoopsie!</h1>
        <p>That link has either been disabled or doesn't exist</p>
        <p>
            <a href="/">Go back to the home page</a>
        </p>
    </div>
</body>
</html>